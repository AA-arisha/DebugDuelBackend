// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

<<<<<<< Updated upstream
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Problem {
  id          Int          @id @default(autoincrement())
  title       String
  description String
  buggyCodes   String     @default("{}") 
  testcases   Testcase[]
=======
// -------------------
// Users (Admins & Participants)
// -------------------
model User {
  id        BigInt   @id @default(autoincrement())
  username  String   @unique        // For admin: username, for participant: teamName
  email     String?                   // For admins or team leaders
  password  String
  role      String                    // "ADMIN" or "PARTICIPANT"
  createdAt DateTime @default(now())

  // Relations (for participants)
  submissions  Submission[]          @relation("UserSubmissions")
  leaderboards Leaderboard[]         @relation("UserLeaderboards")
  emailLogs    EmailLog[]            @relation("UserEmailLogs")
>>>>>>> Stashed changes
}

model Testcase {
  id                Int     @id @default(autoincrement())
  description       String
  problemId         Int
  input             String
  expected          String
  isHidden          Boolean @default(false)
  problem           Problem @relation(fields: [problemId], references: [id])
}

<<<<<<< Updated upstream
=======
// -------------------
// Questions
// -------------------
model Question {
  id               BigInt      @id @default(autoincrement())
  roundId          BigInt
  title            String
  problemStatement String
  createdAt        DateTime    @default(now())

  round       Round        @relation(fields: [roundId], references: [id])
  TestCases   TestCase[]
  BuggyCodes  BuggyCode[]
  Submissions Submission[]
}

// -------------------
// Test Cases
// -------------------
model TestCase {
  id             BigInt   @id @default(autoincrement())
  questionId     BigInt
  input          String
  expectedOutput String
  isHidden       Boolean  @default(false)

  question Question @relation(fields: [questionId], references: [id])
}

// -------------------
// Buggy Codes
// -------------------
model BuggyCode {
  id         BigInt   @id @default(autoincrement())
  questionId BigInt
  language   String
  code       String

  question Question @relation(fields: [questionId], references: [id])
}

// -------------------
// Submissions
// -------------------
model Submission {
  id         BigInt   @id @default(autoincrement())
  userId     BigInt
  roundId    BigInt
  questionId BigInt
  language   String
  code       String
  score      Int
  submittedAt DateTime @default(now())
  timeTakenSeconds Int
  isCorrect  Boolean

  user     User     @relation("UserSubmissions", fields: [userId], references: [id])
  round    Round    @relation(fields: [roundId], references: [id])
  question Question @relation(fields: [questionId], references: [id])
}

// -------------------
// Leaderboards
// -------------------
model Leaderboard {
  id          BigInt @id @default(autoincrement())
  roundId     BigInt
  userId      BigInt
  score       Int    @default(0)
  timePenalty Int    @default(0)
  rank        Int?

  round Round @relation(fields: [roundId], references: [id])
  user  User  @relation("UserLeaderboards", fields: [userId], references: [id])

  @@unique([roundId, userId])
}

// -------------------
// Email Logs
// -------------------
model EmailLog {
  id          BigInt   @id @default(autoincrement())
  userId      BigInt
  email       String
  status      String   // sent | failed
  errorMessage String?
  sentAt      DateTime @default(now())

  user User @relation("UserEmailLogs", fields: [userId], references: [id])
}
>>>>>>> Stashed changes
